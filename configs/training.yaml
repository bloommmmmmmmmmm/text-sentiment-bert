model:
  name: distilbert/distilbert-base-uncase
  num_labels: 6

dataset: dair-ai/emotion

train:
  epochs: 3
  learning_rate: 2e-5
  weight_decay: 0.01
  batch_size: 16

output_dir: models/emotions-distilbert-v1